# dependencies (recursive for monorepo)
node_modules
.pnp
.pnp.js

# testing
coverage

# next.js (recursive for monorepo)
.next
out

# production
build

# turbo cache
.turbo

# large / personal folders (do not commit)
/Archive/
/Media/
/Documents/
/WolfTok/

# npm cache (should never be committed)
.npm-cache/

# audit/artifact outputs (may contain captured secrets)
apps/slctrips/artifacts/

# large app-specific directories
apps/gmc-mag/GMC/
apps/gmc-mag/GMC*/

# large reference files
*.pdf
!docs/clawdbot/*.pdf
*.zip
*.tar.gz
*.mp4

# large image directories (use CDN instead)
**/images/**/Guardians*

# misc
.DS_Store
*.pem

# cursor
.cursorrules

# planning/documentation files (keep in local notes, not git)
/*.md
!README.md
**/VERCEL_MIGRATION.md

# one-off SQL scripts
/*.sql

# root-level images (use proper asset locations)
/*.png

# stray config files
/components.json
/.eslintrc.json

# duplicate pnpm files in wrong locations
apps/slctrips/pnpm-lock.yaml
apps/slctrips/pnpm-workspace.yaml

# app-level npm/yarn lockfiles — root pnpm is source of truth
apps/**/package-lock.json
apps/**/yarn.lock

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
.env.mcp

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# test results
test-results

# LibreOffice lock files
.~lock.*

# root-level outreach data and scripts (contain PII / API keys)
/*.xlsx
/*.csv
/*.docx
/*.cjs
/*.py
/scripts/

# contact data and send results (contain PII)
/*_contacts*.json
/send_results_*.json
.npm-cache/
.npm-cache/
